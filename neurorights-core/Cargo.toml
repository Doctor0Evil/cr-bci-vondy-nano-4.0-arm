[package]
name = "neurorights-core"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[build-dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dependencies]
serde = { version = "1", features = ["derive"] }
